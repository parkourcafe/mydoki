# Never commit the real keystore or passwords.
# The RuStore release must reuse the certificate already used by help.doki.twa.
storeFile=app/signing.keystore
storePassword=REPLACE_ME
keyAlias=REPLACE_ME
keyPassword=REPLACE_ME
