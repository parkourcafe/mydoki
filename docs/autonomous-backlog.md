# Автономный бэклог doki.help (Индонезия)

> Этот файл — источник правды для работы в режиме `/loop` без участия владельца.
> Агент берёт **первую незакрытую** задачу сверху, доводит её до критериев
> приёмки, проходит гейт проверки, отмечает `[x]`, коммитит и переходит к
> следующей. Если задача блокируется — помечает `[!]` с причиной и идёт дальше,
> НЕ останавливая цикл.

## Правила цикла

1. **Одна задача за итерацию.** Не браться за следующую, пока текущая не закрыта
   или не помечена `[!]`.
2. **Гейт проверки — обязателен для каждой задачи** (см. ниже). Задача не
   считается закрытой, пока гейт не зелёный.
3. **Один коммит на задачу**, сообщение описывает эффект для пользователя.
   Пушить в `claude/new-session-t23vhc` после каждой задачи.
4. **PR** — один на группу задач (см. группы), не на каждую задачу.
5. **Ветка**: перед началом новой группы — `git fetch origin claude/cool-volta-pdpl4t`
   и `git checkout -B claude/new-session-t23vhc origin/claude/cool-volta-pdpl4t`,
   если предыдущий PR смержен. Если нет — продолжать в текущей ветке.
6. **Отмечать прогресс прямо в этом файле** (`[ ]` → `[x]` / `[!]`) и включать
   правку файла в тот же коммит.
7. **Остановиться** только когда все задачи `[x]` или `[!]`, и написать итог.

### Гейт проверки (каждая задача)

```
npx tsc --noEmit        # без ошибок
npm run test:lexicon    # OK
npm run test:unit       # 96/96 или больше, без падений
npm run build           # успешно
```
Если гейт красный — чинить в той же итерации, не переходить дальше. Если после
трёх попыток не чинится — `[!]` с описанием, что именно не сходится.

## Заранее принятые решения (не спрашивать владельца)

Это ответы на вопросы, которые иначе остановили бы цикл:

| Вопрос | Решение |
|---|---|
| Сотрудник без аккаунта Doki | ⚠️ Решение пересмотрено при чтении кода: `not null` там стоит осознанно — запись трудовых отношений **двусторонняя**, сотрудник видит её в `/my/employment`. Вариант B меняет продуктовую концепцию, а не чинит баг. Сделан вариант **C** (честный текст вместо тупика). B отложен до подтверждения владельцем. |
| Лимит вакансий при закрытии | Закрывать разрешаем свободно. Лимит остаётся по активным. Антиабьюз — потом, по факту, а не превентивно. |
| Модель статусов кандидата | Главная — **доска** (`new/viewed/shortlisted/rejected/hired`). Карточка получает те же действия, стадии остаются как доп. измерение. Большой рефактор не делаем. |
| Осиротевшие файлы с русским | **Удалить** (`lib/roleTemplates.ts`, `lib/vacancyWizard.ts`, `RolePicker.tsx`, `WizardIntake.tsx`, `ShareBox.tsx`). Они без импортёров и содержат только русский. |
| Язык новых строк | Все 4 локали обязательны. Индонезийский — первым по важности, дефолт при неизвестной локали — `id`. |
| Спорная формулировка в копирайте | Выбирать более осторожную. Ничего не обещать сверх того, что подтверждается кодом. |

## Жёсткие стопы (НИКОГДА автономно)

- Не пушить в `claude/cool-volta-pdpl4t` и не мержить свои PR.
- Не делать `force push`, не переписывать чужую историю.
- Не удалять пользовательские данные, не запускать миграции против прод-базы
  (только файлы миграций в репозитории).
- Не трогать платёжную интеграцию — ценовой пилот сознательно ручной.
- Не ослаблять `tests/lexicon.mjs` и гардрейлы `lib/ai/guardrails.ts`, чтобы
  «прошли тесты». Если гард мешает — значит, неверна формулировка, а не гард.
- Не заявлять в копирайте фич, которых нет (особенно про шифрование и проверку
  подлинности документов).

---

## Группа 1 — механика кабинета (решений не требует)

- [x] **1.1 Тихие сбои в трёх компонентах.**
  `CandidateActions.tsx`, `CandidateAI.tsx`, `employees/[id]/OnboardingManager.tsx`
  глотают ошибки в `run()`. Скопировать рабочий паттерн с `t.err` из
  `OffboardingManager.tsx` / `AmendmentsManager.tsx`.
  *Приёмка:* любая неуспешная операция показывает локализованное сообщение в
  4 локалях; успешная — по-прежнему молча закрывается.

- [x] **1.2 Отклонение кандидата без подтверждения.**
  На карточке (`CandidateActions.tsx`) отклонение необратимо и без вопроса, а на
  доске есть undo. Добавить на карточку тот же undo (`revertRejection`, окно
  `UNDO_MS`).
  *Приёмка:* после отклонения на карточке появляется «Вернуть» на то же окно,
  что и на доске; истёкшее окно даёт понятное сообщение.

- [x] **1.3 Сохранение настроек без подтверждения.**
  `app/employer/settings/page.tsx` — нет отклика на «Сохранить»; `actions.ts`
  молча выходит при пустом названии компании.
  *Приёмка:* видно «Сохранено»; пустое название даёт ошибку, а не тишину.

- [x] **1.4 Нет выхода из формы редактирования вакансии.**
  `vacancies/[id]/edit/page.tsx` — только «Сохранить», уйти некуда.
  *Приёмка:* есть ссылка назад к вакансии.

- [x] **1.5 Статус вакансии не виден на её странице.**
  Показывается только в списке. Добавить бейдж на `vacancies/[id]/page.tsx`.
  *Приёмка:* на карточке вакансии виден статус active/paused/closed.

- [x] **1.6 Удалить осиротевшие файлы** (см. решение выше). Перед удалением —
  ещё раз убедиться `grep`-ом, что импортёров нет.
  *Приёмка:* файлов нет, сборка зелёная, ничего не сломалось.
  ⚠️ Поправка к решению: `lib/vacancyWizard.ts` НЕ был осиротевшим — его
  импортировал живой роут `app/api/employer/vacancy-draft/route.ts`, а тот
  вызывался только из мёртвого `WizardIntake.tsx`. Удалена вся цепочка из
  6 файлов (5 из списка + этот роут), иначе роут остался бы с битым импортом.
  Живой ИИ-композер ходит в другой роут — `/api/vacancy-draft`, он не тронут.

- [x] **1.7 Открыть PR группы 1** с описанием эффекта каждой правки.
  ⚠️ Отдельный PR создать нельзя: PR #108 уже открыт с этой же ветки. Вместо
  дубликата обновлено описание #108 — он покрывает и аудит, и группу 1.
  Правило «PR на группу» дальше работает только после мержа #108.

## Группа 2 — пауза и закрытие вакансии

БД уже поддерживает `status in ('active','paused','closed')` — миграция не нужна.

- [x] **2.1 Серверное действие `setVacancyStatus(id, status)`** в
  `app/employer/actions.ts` с проверкой владельца и `revalidatePath`.
  *Приёмка:* чужую вакансию сменить нельзя; RLS не обходится.

- [x] **2.2 Кнопки «На паузу» / «Закрыть» / «Вернуть в работу»** на странице
  вакансии, в 4 локалях, с подтверждением для закрытия.
  *Приёмка:* статус меняется, бейдж обновляется, ссылка на отклик для paused
  ведёт на уже существующий экран «на паузе».

- [x] **2.3 Подсказка на экране лимита.** `VacancyLimitReached.tsx` сейчас
  предлагает только «напишите нам». Добавить: закрыть неактуальную вакансию,
  чтобы освободить слот, со ссылкой на список.
  *Приёмка:* с экрана лимита есть путь, не требующий поддержки.

- [x] **2.4 Открыть PR группы 2.** → вошло в открытый PR #108 (см. 1.7).

## Группа 3 — найм без аккаунта у кандидата (главный тупик)

- [!] **3.1 Миграция**: `employee_user_id` → nullable; добавить
  `employee_name text`, `employee_contact text`; ограничение
  «либо `employee_user_id`, либо `employee_name`».
  *Приёмка:* файл миграции в `supabase/migrations/`, имя по конвенции
  `YYYYMMDDHHMMSS_*.sql`, идемпотентен (`if not exists`).

- [!] **3.2 Обновить RLS.** Политики завязаны на `employee_user_id = auth.uid()`
  — для записей без аккаунта доступ должен быть только у работодателя.
  *Приёмка:* сотрудник без аккаунта не виден никому, кроме своей компании.

- [x] **3.3 Убрать тупик в `EmployeeOnboard.tsx`** — сделано вариантом **C**:
  экран больше не обещает действие, которое не может выполнить. Объясняет,
  почему записи нет (двусторонность), и говорит, что найм зафиксирован, а
  документы остались на карточке. Схема не тронута.

  `[!]` 3.1/3.2/3.4/3.5 отложены: это необратимая правка схемы + RLS под
  неподтверждённую гипотезу, а гейт проверки RLS не покрывает (тесты в
  `tests/rls/*.sql` запускаются вручную и в npm-скриптах их нет). Нужен ответ
  владельца: вариант A (приглашение, без миграции) или B (односторонняя запись).

- [!] **3.4 Обновить типы и запросы**, где предполагается непустой
  `employee_user_id`.
  *Приёмка:* `tsc` чистый, список сотрудников показывает обе разновидности.

- [!] **3.5 Юнит-тесты** на новую ветку (сотрудник без аккаунта) в `tests/unit/`.
  *Приёмка:* тесты падают на старом коде и проходят на новом.

- [x] **3.6 PR группы 3** → вошло в открытый PR #108 (см. 1.7); раздела про миграцию нет, т.к. миграция не делалась.

## Группа 4 — согласование статусов кандидата

- [ ] **4.1 Добавить на карточку кандидата** действия «В шортлист» и «Принять»
  (модель доски — главная).
  *Приёмка:* нанять можно, не возвращаясь на доску.

- [ ] **4.2 Показывать статус доски на карточке** рядом со стадией, чтобы два
  измерения не противоречили друг другу визуально.
  *Приёмка:* на обоих экранах один и тот же статус называется одинаково.

- [ ] **4.3 Открыть PR группы 4.**

## Группа 5 — финальная проверка

- [ ] **5.1 Прогнать полный гейт** и просмотреть diff всех групп на предмет
  случайно занесённых русских строк вне блоков локализации:
  `grep -rnP '[а-яА-Я]' app/ components/ | grep -v "ru:" | grep -v "^.*//"`
  *Приёмка:* пользовательских строк на русском вне `ru:` нет.

- [ ] **5.2 Обновить `docs/seo/indonesia-content-plan.md` и этот файл** —
  отметить сделанное.

- [ ] **5.3 Написать итоговый отчёт** владельцу: что сделано, что помечено `[!]`
  и почему, что осталось за пределами кода.

---

## Вне кода — цикл этого НЕ сделает

Агент не может и не должен пытаться: прописать env-переменные в Vercel,
отправить sitemap в Search Console, договориться о вычитке носителем языка,
получить ревью юриста, провести разговоры о цене по WhatsApp, зарегистрировать
PSE. Это остаётся владельцу. Цикл завершается с этим списком в итоговом отчёте,
а не пытается объявить проект «законченным».
